{
    "name": "zepio-erp-monorepo",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "client",
        "server"
    ],
    "scripts": {
        "build": "cd client && npm install && npm run build",
        "deploy": "wrangler deploy"
    },
    "devDependencies": {
        "wrangler": "^4.54.0"
    }
}
